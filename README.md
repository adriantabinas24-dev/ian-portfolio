# Ian Portfolio

A minimal React + Vite foundation for a responsive editorial portfolio.

## Development

```sh
npm install
npm run dev
```

## Production build

```sh
npm run build
npm run preview
```

The project intentionally contains no page sections yet. Global design tokens and responsive foundations live in `src/styles/global.css`.
